# Admin login (full access including Settings)
ADMIN_PASSWORD=changeme-admin

# Session signing — use a long random string
AUTH_SECRET=replace-with-long-random-string

# Optional: legacy auto-migrate to data/config.json on first run
# LIBRARY_ROOT=C:\Videos\Movies
# APP_PASSWORD=guest-password
